version: '3'

services:
  gatsby:
    build:
      context: .
      dockerfile: Dockerfile.development
    working_dir: /portfolio
    env_file:
      - .env
    volumes:
      - ./portfolio:/portfolio
      - node_modules_gatsby:/portfolio/node_modules
      - cache:/portfolio/.cache
    network_mode: host
    stdin_open: true
    tty: true
    command: yarn develop

volumes:
  node_modules_gatsby:
  cache:
